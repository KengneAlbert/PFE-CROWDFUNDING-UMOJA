import 'package:flutter/material.dart';

class FundingCardSearch extends StatelessWidget{
  const FundingCardSearch({Key? key}) : super(key: key);

  @override
   Widget build(BuildContext context) {
    return Center();


   }
}